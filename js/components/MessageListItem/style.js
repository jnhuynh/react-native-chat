'use strict';

var React = require('react-native');
var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  container: {
    backgroundColor: '#FEFEFE',
    borderRadius: 5,
    padding: 10,
    margin: 5,
  },
});
